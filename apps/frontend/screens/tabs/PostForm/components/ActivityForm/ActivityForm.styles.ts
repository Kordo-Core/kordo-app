import styled from '@emotion/native';

export const SectionContainer = styled.View((props) => ({
  gap: props.theme.spacing.md,
}));

export const BlocsWrapper = styled.View((props) => ({
  gap: props.theme.spacing.xs,
}));
